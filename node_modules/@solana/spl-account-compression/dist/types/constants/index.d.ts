import { PublicKey } from "@solana/web3.js";
export declare const SPL_NOOP_ADDRESS = "noopb9bkMVfRPU8AsbpTUg8AQkHtKwMYZiFUjNRtMmV";
export declare const SPL_NOOP_PROGRAM_ID: PublicKey;
/**
 * DepthSizePair is a valid (`maxDepth`, `maxBufferSize`) tuple for an SPL ConcurrentMerkleTree
 * Only the tuples listed in {@link ALL_DEPTH_SIZE_PAIRS} are valid for
 * creating a new {@link ConcurrentMerkleTreeAccount}.
 */
export declare type DepthSizePair = {
    maxDepth: number;
    maxBufferSize: number;
};
/**
 * Valid pairs for creating a new {@link ConcurrentMerkleTreeAccount}
 */
export declare const ALL_DEPTH_SIZE_PAIRS: ValidDepthSizePair[];
export declare type ValidDepthSizePair = {
    maxDepth: 3;
    maxBufferSize: 8;
} | {
    maxDepth: 5;
    maxBufferSize: 8;
} | {
    maxDepth: 14;
    maxBufferSize: 64;
} | {
    maxDepth: 14;
    maxBufferSize: 256;
} | {
    maxDepth: 14;
    maxBufferSize: 1024;
} | {
    maxDepth: 14;
    maxBufferSize: 2048;
} | {
    maxDepth: 15;
    maxBufferSize: 64;
} | {
    maxDepth: 16;
    maxBufferSize: 64;
} | {
    maxDepth: 17;
    maxBufferSize: 64;
} | {
    maxDepth: 18;
    maxBufferSize: 64;
} | {
    maxDepth: 19;
    maxBufferSize: 64;
} | {
    maxDepth: 20;
    maxBufferSize: 64;
} | {
    maxDepth: 20;
    maxBufferSize: 256;
} | {
    maxDepth: 20;
    maxBufferSize: 1024;
} | {
    maxDepth: 20;
    maxBufferSize: 2048;
} | {
    maxDepth: 24;
    maxBufferSize: 64;
} | {
    maxDepth: 24;
    maxBufferSize: 256;
} | {
    maxDepth: 24;
    maxBufferSize: 512;
} | {
    maxDepth: 24;
    maxBufferSize: 1024;
} | {
    maxDepth: 24;
    maxBufferSize: 2048;
} | {
    maxDepth: 26;
    maxBufferSize: 512;
} | {
    maxDepth: 26;
    maxBufferSize: 1024;
} | {
    maxDepth: 26;
    maxBufferSize: 2048;
} | {
    maxDepth: 30;
    maxBufferSize: 512;
} | {
    maxDepth: 30;
    maxBufferSize: 1024;
} | {
    maxDepth: 30;
    maxBufferSize: 2048;
};
//# sourceMappingURL=index.d.ts.map